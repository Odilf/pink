structure Set Operations

using Predicate Logic

define "in" as:
	x in { } = true
	x in { x } = true
	x in { x, rest... } = true
	x in { y, rest... } = x in { rest... }





# Experimenting below

define "in":
	x in { } = false
	x in { x, rest... } = true

define "|":
	|{}| = 0
	|{ x }| = 1
	|{ x, rest... }| = 1 + |rest|

# Can we define `rest...` in the language itself?

define "rest" = {
	{ x, rest... }
}