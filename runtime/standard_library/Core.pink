domain { true, false }
reserve { in } # Do I reserve curly braces and parenthesis in the language itself? I certainly have to reserve commas

# Parenthesis unwrapping
(x) = x;

# In operator
x in { x, rest } = true;
x in { x } = true;

## Fallback
x in { y } = false;